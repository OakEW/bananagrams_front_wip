# bananagrams

React + TypeScript port of the lobby page, built with Vite.

## Setup

1. Install dependencies:
   ```
   npm install
   ```

3. Run the dev server:
   ```
   npm run dev
   ```
   Then open the URL it prints (http://localhost:5173).
